import { primaryColor } from "../constants/constants"

export const DarkTheme = {
    dark: true,
    colors:{
        primary: primaryColor,
        background: '#000000',
        text: '#FFFFFF',
    },
    fonts:{
        bold:{
            fontWeight: 'bold',
        }
    }
}